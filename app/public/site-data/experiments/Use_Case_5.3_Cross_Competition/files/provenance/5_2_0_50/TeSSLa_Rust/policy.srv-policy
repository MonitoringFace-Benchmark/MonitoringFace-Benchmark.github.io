-- Generated by MonitoringFace TeSSLaPolicyConverter.
-- Encoding: TeSSLa time = timepoint index; mf_ts carries timestamps;
-- one Set[List[Int]] event per predicate per timepoint.
in mf_ts: Events[Int]
in mf_eof: Events[Unit]
in mf_p_P0: Events[Set[List[Int]]]
in mf_p_P1: Events[Set[List[Int]]]
in mf_p_P2: Events[Set[List[Int]]]
in mf_p_P3: Events[Set[List[Int]]]
in mf_p_Placeholder: Events[Set[List[Int]]]
def mf_tick: Events[Set[List[Int]]] = const(Set.empty[List[Int]], mf_ts)
def mf_s1: Events[Set[List[Int]]] = slift1(merge(mf_p_P0, mf_tick), (s: Set[List[Int]]) => Set.fold(s, Set.empty[List[Int]], (acc: Set[List[Int]], t: List[Int]) => if List.size(t) == 1 then Set.add(acc, List.append(List.empty[Int], List.get(t, 0))) else acc))
def mf_s2: Events[Set[List[Int]]] = slift1(mf_s1, (s: Set[List[Int]]) => Set.map(s, (t: List[Int]) => List.empty[Int]))
def mf_s3: Events[Set[List[Int]]] = merge(last(mf_s2, mf_ts), mf_tick)
def mf_s4: Events[Set[List[Int]]] = slift1(merge(mf_p_P2, mf_tick), (s: Set[List[Int]]) => Set.fold(s, Set.empty[List[Int]], (acc: Set[List[Int]], t: List[Int]) => if List.size(t) == 0 then Set.add(acc, List.empty[Int]) else acc))
def mf_s5: Events[Set[List[Int]]] = slift1(merge(mf_p_P2, mf_tick), (s: Set[List[Int]]) => Set.fold(s, Set.empty[List[Int]], (acc: Set[List[Int]], t: List[Int]) => if List.size(t) == 0 then Set.add(acc, List.empty[Int]) else acc))
def mf_s6: Events[Set[List[Int]]] = slift(mf_s4, mf_s5, (a: Set[List[Int]], b: Set[List[Int]]) => Set.fold(a, Set.empty[List[Int]], (acc: Set[List[Int]], x: List[Int]) => Set.fold(b, acc, (acc2: Set[List[Int]], y: List[Int]) => if true then Set.add(acc2, List.empty[Int]) else acc2)))
def mf_s7: Events[Set[List[Int]]] = slift1(mf_s6, (s: Set[List[Int]]) => if Set.size(s) == 0 then Set.add(Set.empty[List[Int]], List.empty[Int]) else Set.empty[List[Int]])
def mf_s8: Events[Set[List[Int]]] = slift(mf_s3, mf_s7, (a: Set[List[Int]], b: Set[List[Int]]) => Set.fold(a, Set.empty[List[Int]], (acc: Set[List[Int]], x: List[Int]) => Set.fold(b, acc, (acc2: Set[List[Int]], y: List[Int]) => if true then Set.add(acc2, List.empty[Int]) else acc2)))
def mf_v: Events[Bool] = slift1(mf_s8, (s: Set[List[Int]]) => Set.size(s) > 0)
out mf_v
