-- Generated by MonitoringFace TeSSLaPolicyConverter.
-- Encoding: TeSSLa time = timepoint index; mf_ts carries timestamps;
-- one Set[List[Int]] event per predicate per timepoint.
in mf_ts: Events[Int]
in mf_eof: Events[Unit]
in mf_p_P0: Events[Set[List[Int]]]
in mf_p_P1: Events[Set[List[Int]]]
in mf_p_P2: Events[Set[List[Int]]]
in mf_p_P3: Events[Set[List[Int]]]
in mf_p_Placeholder: Events[Set[List[Int]]]
def mf_tick: Events[Set[List[Int]]] = const(Set.empty[List[Int]], mf_ts)
def mf_s1: Events[Set[List[Int]]] = slift1(merge(mf_p_P3, mf_tick), (s: Set[List[Int]]) => Set.fold(s, Set.empty[List[Int]], (acc: Set[List[Int]], t: List[Int]) => if List.size(t) == 1 then Set.add(acc, List.append(List.empty[Int], List.get(t, 0))) else acc))
def mf_s2: Events[Set[List[Int]]] = slift1(merge(mf_p_P3, mf_tick), (s: Set[List[Int]]) => Set.fold(s, Set.empty[List[Int]], (acc: Set[List[Int]], t: List[Int]) => if List.size(t) == 1 then Set.add(acc, List.append(List.empty[Int], List.get(t, 0))) else acc))
def mf_s3: Events[Set[List[Int]]] = slift(mf_s1, mf_s2, (a: Set[List[Int]], b: Set[List[Int]]) => Set.union(a, b))
def mf_s4: Events[Set[List[Int]]] = slift1(merge(mf_p_P2, mf_tick), (s: Set[List[Int]]) => Set.fold(s, Set.empty[List[Int]], (acc: Set[List[Int]], t: List[Int]) => if List.size(t) == 1 then Set.add(acc, List.append(List.empty[Int], List.get(t, 0))) else acc))
def mf_s5: Events[Set[List[Int]]] = slift1(merge(mf_p_P2, mf_tick), (s: Set[List[Int]]) => Set.fold(s, Set.empty[List[Int]], (acc: Set[List[Int]], t: List[Int]) => if List.size(t) == 1 then Set.add(acc, List.append(List.empty[Int], List.get(t, 0))) else acc))
def mf_s6_prev: Events[Set[List[Int]]] = merge(last(mf_s6, mf_ts), mf_tick)
def mf_s6_keep: Events[Set[List[Int]]] = slift(mf_s6_prev, mf_s4, (p: Set[List[Int]], a: Set[List[Int]]) => Set.fold(p, Set.empty[List[Int]], (acc: Set[List[Int]], t: List[Int]) => if Set.contains(a, List.append(List.empty[Int], List.get(t, 0))) then Set.add(acc, t) else acc))
def mf_s6: Events[Set[List[Int]]] = slift(mf_s5, mf_s6_keep, (b: Set[List[Int]], k: Set[List[Int]]) => Set.union(b, k))
def mf_s7: Events[Set[List[Int]]] = slift(mf_s3, mf_s6, (a: Set[List[Int]], b: Set[List[Int]]) => Set.fold(a, Set.empty[List[Int]], (acc: Set[List[Int]], x: List[Int]) => if Set.contains(b, List.append(List.empty[Int], List.get(x, 0))) then acc else Set.add(acc, x)))
def mf_s8: Events[Set[List[Int]]] = merge(last(mf_s7, mf_ts), mf_tick)
def mf_closed: Events[Set[List[Int]]] = slift1(mf_s8, (s: Set[List[Int]]) => Set.map(s, (t: List[Int]) => List.empty[Int]))
def mf_v: Events[Bool] = slift1(mf_closed, (s: Set[List[Int]]) => Set.size(s) > 0)
out mf_v
def mf_setout: Events[Set[List[Int]]] = filter(mf_s8, mf_v)
def mf_cols: Events[String] = default(nil[String], "x2")
out mf_cols
out mf_ts
out mf_setout as mf_set
